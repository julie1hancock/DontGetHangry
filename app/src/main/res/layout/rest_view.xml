<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:id="@+id/rootRest">

    <LinearLayout
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@color/orange"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:id="@+id/navigationLayout"
        android:padding="8dp"
        >
        <ImageView
            android:layout_gravity="center"
            android:src="@color/red"
            android:id="@+id/restLineMarkerIcon"
            android:layout_width="50dp"
            android:layout_height="50dp"
            />
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Click to \nnavigate here"
            android:id="@+id/restLineNavigateButton"
            android:background="@color/transparent"
            />
    </LinearLayout>

    <TextView
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/navigationLayout"
        app:layout_constraintBottom_toBottomOf="parent"

        android:padding="16dp"
        android:id="@+id/restLineRestName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Chick-fil-A"
        />

</androidx.constraintlayout.widget.ConstraintLayout>